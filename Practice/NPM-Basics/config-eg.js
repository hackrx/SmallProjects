module.exports = {
  consumer_key: "Replace it with your API key", // You can Get all these keys by signup as developer from developer.twitter.com
  consumer_secret: "Replace it with your API key", //Then you need to fill a form and answer some basic questions answer them acc. to u
  access_token: "Replace it with your API key", //Then they will verify u and provide u your API key!(Make sure you are using another new twitter acc.
  access_token_secret: "Replace it with your API key", // and make sure you have verified your email-id and connect mobile number with ur acc. otherwise u will no get API key.)
  timeout_ms: 60 * 1000, // optional HTTP request timeout to apply to all requests.
  strictSSL: true // optional - requires SSL certificates to be valid.
};
